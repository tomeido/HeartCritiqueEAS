## 2025-01-20 - Add fallback CTA for empty states
**Learning:** When a user filters a list and gets no results, they are stuck in a dead-end state and have to manually interact with the filter again to recover.
**Action:** Always provide a one-click recovery path, such as a "View All" CTA, in the empty state of filtered lists to reduce friction and improve the experience.